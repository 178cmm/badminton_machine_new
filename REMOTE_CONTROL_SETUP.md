# 🎮 簡報筆遙控功能設定指南

## 📋 功能概述

本系統已成功實作「簡報筆遙控：一鍵開始/暫停」的 MVP 功能，支援：

- ✅ 空白鍵（Space）開始/暫停切換
- ✅ Esc 鍵急停保護
- ✅ 顯眼狀態燈顯示
- ✅ 統一控制入口
- ✅ 500ms 防抖機制
- ✅ 完整審計記錄
- ✅ 智能訓練目標檢查
- ✅ 多種訓練模式支援

## 🚀 快速開始

### 1. 啟動主程式
```bash
cd /Users/duyanze/Desktop/coding_1
python main.py
```

### 2. 使用簡報筆遙控
1. 插入 2.4GHz USB 接收器
2. 確保應用視窗保持前景聚焦
3. 按簡報筆的「播放/空白鍵」開始/暫停
4. 按簡報筆的「停止鍵」急停
5. 觀察主視窗下方的狀態燈變化

### 3. 智能檢查機制
- **自動檢查**：系統會自動檢查是否有訓練目標
- **防止誤操作**：沒有選擇套餐/位置時不會開始發球
- **明確提示**：會顯示需要先選擇的訓練類型

### 4. 狀態燈說明
- 🟡 **待機**：黃底黑字「💤 待機」
- 🟢 **發球中**：綠底白字「🚀 發球中」  
- 🔴 **急停**：紅底白字「🛑 急停」

## 🧪 測試功能

### 自動化測試
```bash
# 執行狀態機和事件記錄測試
python tools/sim_e2e.py
```

### 互動式測試
```bash
# 啟動測試視窗（可模擬鍵盤事件）
python test_remote_control.py
```

## 📁 檔案變更摘要

### 新增/修改的檔案

1. **`core/services/system_service.py`** - 擴充狀態機功能
   - 新增 `State` 枚舉（IDLE/RUNNING/EMERGENCY_STOP）
   - 新增 `toggle()`, `start()`, `pause()`, `stop()` 方法
   - 內建 500ms 防抖機制

2. **`gui/main_gui.py`** - 主視窗功能擴充
   - 新增快捷鍵綁定（空白鍵/Esc 鍵）
   - 新增狀態燈顯示
   - 新增統一控制入口 `toggle_start_pause()`, `emergency_stop()`
   - 新增事件記錄功能 `log_event()`

3. **`gui/ui_course.py`** - 課程訓練按鈕整合
   - 修改開始/停止按鈕連接到統一入口

4. **`gui/ui_control.py`** - 手動控制按鈕整合
   - 修改連發控制按鈕連接到統一入口

5. **`tools/sim_e2e.py`** - 測試功能擴充
   - 新增狀態機測試
   - 新增防抖機制測試
   - 新增事件記錄測試

6. **`README.md`** - 文件更新
   - 新增外接遙控功能說明
   - 新增使用指南

7. **`test_remote_control.py`** - 新增測試腳本
   - 互動式測試視窗
   - 鍵盤事件模擬

## 🔧 技術細節

### 狀態機設計
```python
class State(Enum):
    IDLE = "IDLE"           # 待機狀態
    RUNNING = "RUNNING"     # 發球中
    EMERGENCY_STOP = "EMERGENCY_STOP"  # 急停
```

### 防抖機制
- 500ms 防抖時間，避免簡報筆重複觸發
- 可調整：修改 `SystemService._debounce_ms` 屬性

### 事件記錄格式
```json
{
  "event": "TOGGLE_START_PAUSE",
  "state": "RUNNING", 
  "timestamp": 1758938368,
  "source": "keyboard",
  "extra": {"source": "keyboard"}
}
```

## 🎯 使用場景

### 智能工作流程
1. **選擇訓練目標**：
   - 課程訓練：選擇套餐
   - 手動控制：選擇發球位置
   - 基礎訓練：選擇球路
2. **按簡報筆開始**：系統檢查目標後開始訓練
3. **按簡報筆暫停**：隨時暫停訓練
4. **按簡報筆急停**：緊急停止所有訓練

### 簡報筆遙控
1. 教練使用簡報筆遠距離控制發球機
2. 學員專注於接球，無需分心操作界面
3. 緊急情況可快速急停
4. 智能檢查防止誤操作

### 鍵盤快捷鍵
1. 開發者快速測試功能
2. 鍵盤用戶的便捷操作
3. 自動化腳本整合

## 🔮 後續擴充

### 已預留的擴充點
- 全域熱鍵支援（需 macOS 輔助使用權限）
- BLE/GATT 遙控支援
- 自製 ESP32 遙控器

### 可調整參數
- 防抖時間：`SystemService._debounce_ms`
- 狀態燈樣式：`update_status_light()` 方法
- 事件記錄格式：`log_event()` 方法

## ⚠️ 注意事項

1. **視窗聚焦**：遙控功能需要應用視窗保持前景聚焦
2. **相容性**：支援所有標準 2.4GHz USB 簡報筆
3. **日誌檔案**：自動建立 `logs/` 目錄和 `commands.jsonl` 檔案
4. **錯誤處理**：寫檔失敗不會影響主要功能，僅記錄警告

## 🐛 故障排除

### 匯入錯誤
- **QKeySequence 匯入錯誤**：已修復，`QKeySequence` 從 `PyQt5.QtGui` 匯入
- 如果遇到其他匯入錯誤，請確認 PyQt5 版本：`pip install PyQt5>=5.15.0`

### 遙控無效
- 檢查視窗是否聚焦
- 檢查 USB 接收器是否正常
- 檢查簡報筆電池電量

### 狀態燈異常
- 重啟應用程式
- 檢查系統服務狀態
- 查看日誌檔案錯誤訊息

### 記錄缺失
- 檢查 `logs/` 目錄權限
- 檢查磁碟空間
- 查看控制台錯誤訊息
